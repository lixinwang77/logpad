# Logpad 版本历史

## 版本规范

采用语义化版本 `MAJOR.MINOR.PATCH`（例如 `1.2.3`）：

| 变更类型 | 版本号变化 | 示例 |
|----------|------------|------|
| Bug 修复 | PATCH +1 | 1.0.0 → 1.0.1 |
| 新功能（向后兼容） | MINOR +1，PATCH 归零 | 1.0.1 → 1.1.0 |
| 不兼容的重大变更 | MAJOR +1，MINOR/PATCH 归零 | 1.1.0 → 2.0.0 |

每次发版还需递增 **Build 号**（`CURRENT_PROJECT_VERSION`），与版本号同步更新。

### 发版步骤

1. 修改 `Logpad/Logpad.xcodeproj/project.pbxproj` 中的 `MARKETING_VERSION` 和 `CURRENT_PROJECT_VERSION`（Debug / Release 两处都要改）
2. 在本文件「变更记录」中追加条目
3. 提交代码

## 当前版本

**1.14.0** (Build 36)

## 变更记录

### 1.14.0 (2026-06-10)

- 新增超大文件索引进度条：打开文件 ≥ 20MB 时，主视图显示确定性进度条（`LoadingView`），按「已扫描字节 / 文件总大小」实时展示百分比与文件大小，节流约每前进 1% 刷新一次，避免多 GB 文件刷屏；小文件秒级完成不显示进度条以免闪烁。`FileReader` 新增 `fileSize` 与 `indexProgress` 状态

### 1.13.9 (2026-06-10)

- 修复主窗格中含制表符（TAB）的日志行（如缩进的堆栈跟踪）在 TAB 之后内容被整段截断不显示的问题：为每行文本设置段落样式（清空默认制表位、固定 `defaultTabInterval`、`.byClipping` 不截断），完整渲染含 TAB 的行

### 1.13.8 (2026-06-08)

- 修复首次启动后立即「打开文件」时，引导页窗口随打开面板出现而闪退的问题：启动后的空窗口清理（`pruneIdleWindows`）在有窗口挂着 sheet（如打开面板）时不再执行，并把 `NSPanel`/sheet 类窗口排除出判断，避免误把打开面板当成文件窗口而关闭其父窗口

### 1.13.7 (2026-06-08)

- 清理 Xcode 编译警告：将 `LogLine` / `FilterResult` / `ByteNeedle` 标记为 `nonisolated`，并在后台搜索回主线程前拷贝为不可变值，消除 `SearchEngine` 的主线程隔离与并发捕获警告；删除资源目录中未引用的重复 `icon_1024.png`，修复 App 图标「unassigned child」警告；`Info.plist` 拖放回调显式忽略返回值

### 1.13.6 (2026-06-08)

- 修复 macOS 窗口状态恢复导致每次启动累积多个空状态窗口的问题：关闭窗口恢复（`NSQuitAlwaysKeepsWindows=false`），并在启动/打开文件后自动清理多余的空闲独立窗口（最多保留一个引导页，已显示文件时不保留）

### 1.13.5 (2026-06-08)

- 修复 Logpad 已显示文件时「打开方式」打开新文件，文件已进入新标签页却仍多出一个空状态独立窗口的问题；在打开后短时间内多次清理 SwiftUI 误弹的空窗口

### 1.13.4 (2026-06-05)

- 修复 Logpad 已显示文件时，Finder「打开方式」打开新文件会另起独立窗口的问题；现在统一并入当前窗口的新标签页

### 1.13.3 (2026-06-05)

- 修复冷启动 + Finder「打开方式」时，窗口里多出一个空状态标签页的问题；现在复用初始空窗口直接打开文件

### 1.13.2 (2026-06-05)

- 修复 Logpad 已打开文件时，Finder「打开方式」会弹出空白新窗口而非在新标签页中打开文件的问题

### 1.13.1 (2026-06-05)

- 修复 Finder 双击或「打开方式」启动 Logpad 时未自动打开日志文件的问题；注册 `.log` / `.txt` 等纯文本文档类型

### 1.13.0 (2026-06-05)

- 新增多窗口/多标签页支持：`Cmd+T` 在当前窗口新建标签页，`Cmd+N` 打开新的独立 app 窗口；新窗口/标签页各自拥有独立的文件与搜索状态
- 配套修正多窗口下的串扰：窗口标题、文件打开（`Cmd+O`）、聚焦搜索（`Cmd+F`）、跳转行号（`Cmd+G`）、`Cmd+M`/`Cmd+Shift+M` 标记、`Shift+Enter` 上一条匹配等均只作用于当前激活窗口

### 1.12.1 (2026-06-04)

- 新增 App 图标（透明背景 PNG）

### 1.12.0 (2026-06-04)

- 过滤结果面板标题从"结果"改为"过滤结果"

### 1.11.0 (2026-06-04)

- 过滤结果面板标题栏新增显示当前日志文件的完整路径（位于"过滤结果"之前，字体一致）

### 1.10.1 (2026-06-04)

- 修复使用搜索功能后窗口标题被重置为"Logpad"的问题：当 splitMode 变化（none ↔ horizontal/vertical）时，macOS 内部会重建视图层次结构并重置窗口标题，现在通过监听 splitMode 变化来恢复正确的文件名标题

### 1.10.0 (2026-06-02)

- 搜索过滤词后若当前为单窗口模式，自动切换为上下分割（`VSplitView`）显示过滤结果面板；用户已处于任意分屏模式时不改变当前布局；清空搜索词不会自动关闭已打开的分屏

### 1.9.0 (2026-06-01)

- 新增文件编码自动检测：打开时按 64KB 头采样做严格解码，先按 BOM / UTF-8 识别；否则同时尝试 GB18030（GBK 超集）与 Big5 并按解码质量评分（CJK Unified Ideographs +1，非 ASCII 非 CJK × -3 罚分），高分者胜出；GB18030 解 Big5 字节会落到日文假名 / 杂项符号而被罚低，从而正确识别 Big5。检测结果用于 `readLine` 与 `SearchEngine`，非 UTF-8 编码下中文日志可正常显示与搜索；工具栏右端显示当前编码名（如 `UTF-8` / `GB18030` / `Big5`）

### 1.8.0 (2026-06-01)

- 新增 `Cmd+Shift+M` 快捷键：取消当前选中文本的标记（`Cmd+M` 添加标记的逆操作，按文本不区分大小写匹配）

### 1.7.0 (2026-06-01)

- 新增取消文本标记功能：右键菜单「取消标记」子菜单按颜色列出在用标记色（带色块），点击即移除该颜色全部标记，并提供「清除全部标记」；无选区时只要存在标记也可右键调出

### 1.6.4 (2026-06-01)

- 修复输入搜索词后第一个匹配不显示深橙色聚焦高亮的问题：搜索完成即自动聚焦第一个匹配（此前需按一次 `Enter` 才会出现聚焦色，且会跳过第一个）

### 1.6.3 (2026-06-01)

- 修复在搜索框按 `Enter` 后焦点离开搜索框、导致再次按 `Enter` 无法跳转到下一个匹配的问题（提交后重新保持搜索框焦点）

### 1.6.2 (2026-06-01)

- 修复过滤结果面板「结果」计数显示的是匹配行数而非匹配总数的问题：现在显示匹配总数（同一行多个匹配也计入），列表仍按行去重显示

### 1.6.1 (2026-06-01)

- 调整工具栏分屏按钮顺序为「单窗口 / 上下分割 / 左右分割」（上下分割使用频率更高，前置更顺手）

### 1.6.0 (2026-06-01)

- 搜索选项 Regex / Aa（区分大小写）的开关状态现在会持久化保存，重启 app 后保持上次设置（搜索关键词仍为临时，不保存）

### 1.5.0 (2026-06-01)

- 过滤结果区的内容预览现在也会高亮匹配片段（黄色背景），与主视图保持一致

### 1.4.1 (2026-06-01)

- 修复过滤结果区中同一行含多个匹配词时被重复列出的问题：结果列表现在每个匹配行只显示一条，同时保留 `Enter`/箭头逐个匹配跳转的能力（点击结果行聚焦该行第一个匹配）

### 1.4.0 (2026-06-01)

- 上下分割布局的分割线现在也可拖动调整两区占比（改用 `VSplitView`），与左右分割行为一致

### 1.3.2 (2026-05-29)

- 修复使用文本标记（Mark）时 CPU 飙升至 100% 的问题：取消对整个文件的全量预扫描，改为在渲染可见行时即时计算标记命中范围，标记操作不再随文件大小产生开销

### 1.3.1 (2026-05-29)

- 修复在日志中选中文字后点击其他行时，旧选区残留为灰色框的问题（焦点切换到新行时清除上一行选区）

### 1.3.0 (2026-05-29)

- 当前导航到的搜索匹配以更深的黄橙色高亮，区别于其余普通黄色匹配，便于辨认聚焦位置；点击过滤结果列表中的匹配也会聚焦并加深

### 1.2.1 (2026-05-29)

- 修复同一行存在多个匹配时只高亮第一个、且 `Enter`/箭头无法跳转到同行后续匹配的问题（现在每个匹配都高亮并可逐个导航）

### 1.2.0 (2026-05-29)

- 搜索框右侧新增上/下箭头按钮，点击跳转上一条/下一条匹配，鼠标悬停显示对应快捷键（`⇧↵` / `↵`），无结果时禁用

### 1.1.2 (2026-05-29)

- 修复空状态/错误页本地化 key 不匹配（`dragDropHint` / `openFile` / `tryAgain` 显示为原始 key），并将拖放提示改为含 `⌘O` 快捷键的完整文案

### 1.1.1 (2026-05-29)

- 搜索框占位文本显示快捷键提示（`Search (⌘F)` / `搜索 (⌘F)`）

### 1.1.0 (2026-05-29)

- 新增外部文件修改检测：文件被外部写入/删除/替换时显示提示条，可一键重新加载（`Cmd+R`，保持滚动位置）或忽略

### 1.0.0 (2026-05-29)

- 初始发布：文件浏览、搜索高亮、文本标记、分屏布局、跳转行号、中英文界面
- 添加应用版本号与「关于 Logpad」窗口
